# fmt: off
import os
import sys

# The following line adds the "src" directory to sys.path.
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..', "src")))

# Replace "python_package" with the name of the file in "src".
import python_package

# fmt: on

